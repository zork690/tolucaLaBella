export interface ImagenDTO {

    numImagen: number;
    imagen:	any;
    file?: any;
    descripcion: string;
    precio:number;
    check: boolean;
    
    
}

export interface ImagenStringDTO{
    numImagen: number;
    imagen:    string;
}